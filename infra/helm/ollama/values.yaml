enabled: true

image:
  repository: ollama/ollama
  tag: "0.5.7"
  pullPolicy: Always

models:
  baked: false
  pull:
    enabled: true
  list:
    - llama3.2:1b

storage:
  size: 20Gi
  storageClassName: ""

service:
  port: 11434

resources:
  requests:
    cpu: "1"
    memory: 4Gi
  limits:
    memory: 12Gi
