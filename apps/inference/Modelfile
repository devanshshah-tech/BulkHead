# Base model for grounded answer generation (small enough for free-tier VMs).
# Pulled at bundle-build time and shipped inside the Zarf package so the
# airgapped deployment never needs to reach a model registry.
FROM llama3.2:3b

PARAMETER temperature 0.2
PARAMETER num_ctx 4096
SYSTEM Answer only from the provided context chunks and cite them as [n].
